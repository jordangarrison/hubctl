# Deferred Minor review findings (post-merge follow-ups)

From the pre-cutover review (2026-06-23). Critical+Important were fixed before cutover; these Minor items are tracked here.

- **[repos/parity] topics: reads current topics from a different GitHub route than the Ruby** — src/services/repos.ts:353-358 (Ruby lib/hubctl/repos.rb:196-220, github_client.rb:158-162)
  - Ruby reads current topics from the main repository object: `github_client.repository(repo)` then `current_repo[:topics]` (GET /repos/{owner}/{repo}, whose payload includes a `topics` array). The TS port instead issues a dedicated `GET /repos/{owner}/{repo}/topics` request. Both r
- **[repos/parity] list: pretty-table column set diverges (full_name shown vs hidden); JSON parity holds** — src/services/repos.ts:278-296 / src/cli/repos.ts:39-61 (Ruby lib/hubctl/repos.rb:35-48)
  - Ruby builds repo_data WITH `full_name` but passes `headers: %w[name private description language stars forks updated]` (repos.rb:48), so in table mode `full_name` is intentionally omitted from the rendered columns. In JSON mode Ruby ignores headers and emits all fields incl. full
- **[repos/parity] list: success count message ('Found N repositories in <source>') not reproduced** — src/cli/repos.ts:46-61 (Ruby lib/hubctl/repos.rb:50-51)
  - Ruby emits a trailing `formatter.success("Found #{repos.length} repositories in #{source}")` where source is 'organization <org>' or 'your account'. The TS envelope carries only the rows array and next_actions; there is no count/source summary line. Expected under the single-enve
- **[repos/parity] archive: pretty-mode confirm default differs / cancelled-result shape is new** — src/cli/repos.ts:149-189 (Ruby lib/hubctl/repos.rb:166-184, base_command.rb:81-84)
  - Behavior is largely an improvement: Ruby's confirm_action would block on a TTY prompt even in --format json (no json gating), whereas TS correctly never prompts in json mode and returns an ok:false {code:'confirmation_required', fix:'re-run with --yes'} — good. Two small divergen
- **[repos/parity] topics modify: success/info messaging dropped, but merge logic matches** — src/services/repos.ts:207-218 (Ruby lib/hubctl/repos.rb:194-228)
  - mergeTopics correctly mirrors the Ruby merge: `set` replaces outright; otherwise concat add, subtract remove, then uniq (repos.rb:199-206 vs repos.ts:211-218). Verified equivalent including dedupe ordering (current-first). Divergence is only in surfaced text: Ruby prints `formatt
- **[orgs/parity] invite default role `direct_member` dropped** — src/cli/orgs.ts:68 (inviteRoleFlag Flag.optional, no default) and src/services/orgs.ts:216-219; Ruby lib/hubctl/users.rb:70-71,78
  - Ruby's invite sets `method_option :role, default: 'direct_member'` and always sends `{ role: options[:role] }` in the invitation body. The TS flag is optional with no default, and inviteOptions omits `role` entirely when absent (orgs.ts:216-219), so no role is sent. GitHub server
- **[orgs/parity] members `--filter` enum replaced by a `--2fa-disabled` boolean** — src/cli/orgs.ts:47-50,52 and src/services/orgs.ts:201-204; Ruby lib/hubctl/orgs.rb:79,86
  - Ruby exposes `--filter` (default 'all', desc values all|2fa_disabled); when not 'all' it forwards `filter` as a query param. TS instead exposes a boolean `--2fa-disabled` flag that maps to filter=2fa_disabled. Functionally equivalent for the only meaningful non-default value, but
- **[orgs/parity] disk_usage null formatting differs ('0 KB' vs ' KB')** — src/services/orgs.ts:172; Ruby lib/hubctl/orgs.rb:65
  - Ruby formats `"#{organization[:disk_usage]} KB"`, yielding ` KB` (empty) when disk_usage is nil/absent. TS uses `${org.disk_usage ?? 0} KB` -> `0 KB`. Cosmetic divergence in the `show` detail view when disk_usage is not returned (e.g. non-admin orgs).
- **[orgs/parity] `orgs list` schema requires fields that GET /user/orgs omits** — src/services/orgs.ts:110-119,242-244; Ruby lib/hubctl/orgs.rb:15-26
  - `list` decodes the GET /user/orgs payload with OrgSummary requiring public_repos/public_gists/followers/following as Schema.Finite (non-nullable). The real GET /user/orgs (Octokit organizations) returns MINIMAL org objects that do NOT include these profile counts, so decodeSummar
- **[users/parity] invite `role` default `direct_member` not sent when --role omitted** — src/cli/users.ts:64 (inviteRoleFlag) + src/services/users.ts:195-198 (inviteOptions); Ruby lib/hubctl/users.rb:70-71,78
  - Ruby declares `method_option :role, default: 'direct_member'` and always merges `{ role: options[:role] }` into the invitation body (lib/hubctl/users.rb:70-71,78). TS makes role optional with no default and omits it from the body when absent (inviteOptions skips role when undefin
- **[users/parity] whoami plan/disk_usage handling diverges in edge cases (null-safe vs Ruby raw)** — src/services/users.ts:149-158 (toWhoami); Ruby lib/hubctl/users.rb:129-138
  - Ruby reads `user[:plan][:name]` and `"#{user[:disk_usage]} KB"` with no nil guards (lib/hubctl/users.rb:134,137); a user payload without a `plan` would raise in Ruby, and a nil disk_usage renders as ' KB'. TS makes plan/private_repos/collaborators/disk_usage optional+nullable and
- **[users/bug] remove confirmation prompt message and default differ from Ruby** — src/cli/users.ts:104,125; Ruby lib/hubctl/users.rb:104 via base_command.rb:80-83
  - Ruby prompts `"Are you sure you want to remove #{username} from #{org}?"` with `confirm_action` default false (base_command.rb:80-83). TS prompts `"Remove ${user} from ${org}? This cannot be undone easily."` via Prompt.confirm and treats a quit as decline. Behaviorally equivalent
- **[users/bug] list role enum is restricted to a closed choice set vs Ruby free-form string** — src/cli/users.ts:44 (roleFlag choice); Ruby lib/hubctl/users.rb:7
  - Ruby `method_option :role, default: 'all'` accepts any string and passes it through to octokit unless it equals 'all' (lib/hubctl/users.rb:14). TS restricts --role to choice ['all','admin','member'] (src/cli/users.ts:44). These are GitHub's only valid org-member roles, so TS is s
- **[teams/parity] `teams members` and `teams list` paginate where Ruby/octokit returns a single page by default** — src/services/teams.ts:151,164 — Ruby ref: lib/hubctl/github_client.rb:108-109,120-121
  - TS `list` (src/services/teams.ts:151) and `members` (line 164) use `github.paginate(...)`, fetching all pages. Ruby calls octokit `organization_teams(org)` and `team_members(team_id)` (github_client.rb:108,120) with no `auto_paginate` enabled, so by default they return only the f
- **[teams/bug] `teams add` reports the API-readback role/state; Ruby reports the requested role** — src/services/teams.ts:163-173 — Ruby ref: lib/hubctl/teams.rb:138-139
  - Ruby `add_member` prints `Role: #{options[:role]}` (the role the user requested, teams.rb:139) and never surfaces a membership `state`. The TS `add` (src/services/teams.ts:166-172) reads `role` and `state` back from the PUT response (`decodeMembership`). This is an enhancement (m
- **[enterprise/parity] `members --role` enum differs from Ruby's documented roles; `billing_manager` silently no-ops** — src/cli/enterprise.ts:206 / src/services/enterprise.ts:646-656; Ruby lib/hubctl/enterprise.rb:120, github_client.rb:373-385
  - Ruby method_option :role documents (member, admin, billing_manager) (enterprise.rb:120) and filter_by_role handles admin/owner/member, everything else (incl. billing_manager) falls through to no filtering (github_client.rb:382-384). TS roleFlag is a Flag.choice with ['all','admin
- **[enterprise/bug] `security` update drops the never-used `dependency_graph_..._value` / value-string options** — src/cli/enterprise.ts:561-572; Ruby lib/hubctl/enterprise.rb:512-519,531-534
  - Ruby `security` declares method_option :dependency_graph_enabled_for_new_repositories_value (string, enabled/disabled/not_set) at enterprise.rb:514, but update_options (enterprise.rb:531-534) NEVER reads it — only the three boolean flags are forwarded. TS omits the *_value flag e
- **[enterprise/bug] Security-analysis route is faithfully ported but is not a real GitHub API path (pre-existing Ruby issue)** — src/services/enterprise.ts:765-776; Ruby lib/hubctl/github_client.rb:291-301
  - Both Ruby (github_client.rb:292,298) and TS (services/enterprise.ts:767,772) use GET/PATCH /enterprises/{enterprise}/code_security_analysis. GitHub's documented endpoint is /enterprises/{enterprise}/code-security-and-analysis (or the newer code_security configurations API). The T
- **[enterprise/parity] `owners` reshaped from flat command to `owners list`; Ruby owner rows hardcode role='owner', TS uses determineRole** — src/cli/enterprise.ts:240-251 / src/services/enterprise.ts:778-782; Ruby lib/hubctl/enterprise.rb:156-184, github_client.rb:228-234,391-404
  - Ruby `owners` is a flat top-level command (enterprise.rb:157) and its owner_data hardcodes role:'owner' for every row (enterprise.rb:171). The TS `owners list` runs transformMember on each owner, which sets role via determineRole => 'admin' for users with the 'Owner' role (servic
- **[effect-correctness/parity] `enterprise stats` emits raw payload instead of the Ruby's structured sections** — src/services/enterprise.ts:760-763, src/cli/enterprise.ts:533-544; Ruby ref lib/hubctl/enterprise.rb:432-509
  - Ruby `enterprise stats` formats stats into labeled sections (repos/hooks/pages/orgs/users/pull_requests/issues/milestones/gists). The TS `stats` returns `decodeRawObject` verbatim (the whole `/stats/all` payload). For the JSON envelope this is arguably better (more data), and the
- **[effect-correctness/parity] `users invite` no longer defaults role to `direct_member`** — src/cli/users.ts:64, src/services/users.ts:195-198; Ruby ref lib/hubctl/users.rb:70-71,78
  - Ruby `users invite` sets `method_option :role, default: 'direct_member'` and always sends `role: options[:role]`. TS `inviteRoleFlag` is `Flag.optional` with no default, and `inviteOptions` only includes `role` when supplied, so a bare `hubctl users invite x@y.com` sends no role 
- **[effect-correctness/bug] Stream intermediate NDJSON lines are raw JSON even in pretty mode** — src/output/service.ts:80-84 (writeStream), src/cli/enterprise.ts:447-457
  - writeStream always writes each event via `Console.log(encodeJson(event))` regardless of mode; only the terminal envelope is mode-aware (rendered via `render`). So `enterprise audit-log` in pretty/TTY mode prints compact JSON event lines followed by a pretty terminal envelope. The
- **[envelope-security-deviations/bug] orgs.list exposed as a bare Effect property, not a () => Effect thunk (deviation c)** — src/services/orgs.ts:101 (CLI use: src/cli/orgs.ts:22)
  - `OrgsShape.list` is `Effect.Effect<...>` (a value) while every other service method is a function. Same pattern for `users.whoami` (services/users.ts:91) and `auth.status`. It works and is referentially fine because the effect is re-runnable, but it is inconsistent with the rest 
- **[envelope-security-deviations/bug] `users` committed as one commit vs per-subcommand (deviation b)** — src/cli/users.ts:1 (process/history observation)
  - The `users` group landing as a single commit (vs per-subcommand like other groups) is a process/reviewability nit, not a runtime issue. No correctness or parity impact; the code itself mirrors users.rb for show/whoami/list and (with the invite/remove duplication noted separately)
- **[envelope-security-deviations/bug] version/root emit envelope command names that don't match the invocation path** — src/cli/version.ts:50 / src/cli/root.ts:80
  - Subcommands emit dotted names (`repos.list`, `enterprise.sso.show`). The root handler emits command `'root'` (root.ts:80) and the version handler emits `'version'` (fine), but `'root'` is not a real command path a caller could re-invoke (the binary is `hubctl`). An agent keying o
- **[envelope-security-deviations/bug] Security/secret handling reviewed: no token leakage found** — src/github/errors.ts:86 / src/github/client.ts:46
  - Positive finding. toGithubError builds messages only from `err.message`, status, x-ratelimit headers, and 422 field errors — never the token. The token is injected into Octokit (`auth: options.token`) and never echoed into any envelope, log, or error. Fix strings are generic ('Se
- **[test-quality/parity] enterprise security-analysis update cannot send explicit false (truthy-only boolean flags)** — src/cli/enterprise.ts:561-576 (Ruby lib/hubctl/enterprise.rb:512-534)
  - Ruby method_option type: :boolean is tri-state (nil/true/false); update_options[:x] = options[:x] if options[:x] only forwards truthy, but the flag could carry false via --no-... . The TS dependencyGraph/secretScanning/secretScanningPush flags default to false and flagWhenSet onl
- **[test-quality/parity] teams members/add/remove skip the team-slug existence lookup and lose the friendly not-found message** — src/services/teams.ts:166-199 (Ruby lib/hubctl/teams.rb:78-83,150-157)
  - Ruby fetches organization_teams, finds by slug, and on miss prints "Team '<slug>' not found in organization '<org>'" without calling the member/membership API. TS hits the slug-scoped endpoints directly, so an unknown slug yields a generic NotFoundError ('Check the name and that 
- **[test-quality/parity] users invite drops the Ruby default role 'direct_member' and the 'Role:' info line** — src/cli/users.ts:64,95-98 (Ruby lib/hubctl/users.rb:70-78,86)
  - Ruby defaults --role to 'direct_member' and always sends role + prints info("Role: #{role}"). TS inviteRoleFlag is Flag.optional with no default, so when omitted no role is sent. GitHub server-defaults to direct_member so the API outcome matches, but the envelope no longer reflec
- **[test-quality/parity] orgs gains invite/remove subcommands that do not exist on the Ruby orgs command** — src/cli/orgs.ts:89,137 (Ruby lib/hubctl/orgs.rb)
  - Ruby Orgs has only list/show/members/repos/teams/info; invite & remove live solely on Users. The TS orgs.ts adds invite and remove subcommands (duplicating users.invite/remove logic in orgs.ts service). Additive, not a regression, but it diverges from the Ruby command surface and
- **[test-quality/parity] orgs/users 'repos' and 'info' Ruby commands not ported to the orgs group** — src/cli/orgs.ts:165 (Ruby lib/hubctl/orgs.rb:112-201)
  - Ruby `orgs repos` (org repositories list) and `orgs info` (authenticated user's org memberships summary) have no equivalent under the TS orgs command group (orgs subcommands are list/show/members/invite/remove). `repos list --org` covers the org-repos case, but `orgs info` (whoam
- **[test-quality/parity] Generic/unknown GitHub errors mapped to ValidationError tag instead of a generic API error** — src/github/errors.ts:120 (Ruby lib/hubctl/github_client.rb:463)
  - Ruby handle_generic_api_error raises APIError 'GitHub API error: ...'. The TS toGithubError fallback for any non-401/403/404/422/429 status returns a ValidationError ('Fix the highlighted fields and retry' is replaced by 'Unexpected GitHub error; retry or report this'). A 500/502
